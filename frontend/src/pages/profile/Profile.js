import React from "react";
import { Link } from "react-router-dom";

const Profile = (props) => {
    
    return (
        <div>
          <Link className="align-self-center" >
            <p> Sorry, the user doesn't have a profile yet. </p>
          </Link>
         </div>
    );
  };
  
  export default Profile;